# SQL Database Assignment

This repository contains my submission for the **Introduction to Databases** assignment.

## 📘 Objectives

- Learn to create and manage SQL databases
- Understand basic SQL commands such as `CREATE DATABASE` and `DROP DATABASE`
- Practice using SQL in a clean and structured way

## 📁 Files

- `answers.sql` — Contains the SQL queries for:
  1. Creating a database called `salesDB`
  2. Dropping a database called `demo`

## 🛠️ Technologies Used

- SQL (Standard)
- Text Editor: Visual Studio Code

## ✅ Instructions

To run the SQL queries:

1. Open your preferred SQL editor (e.g., MySQL Workbench, pgAdmin, or CLI).
2. Load the `answers.sql` file.
3. Run each query as needed.

## 📌 Notes

- Make sure you have the correct privileges to create or drop databases.
- This is a beginner-level assignment to practice basic database management.

---

